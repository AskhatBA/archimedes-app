import { StyleSheet } from 'react-native';

export const createAppointmentFormStyles = StyleSheet.create({
  title: {
    fontWeight: 600,
    fontSize: 18,
    lineHeight: 22,
    letterSpacing: 0,
    marginBottom: 11,
  },
});
